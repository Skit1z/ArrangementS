# 2026-07-22

## 提交后端 CI 提速改动（第九波）

- commit: `a94314d` → `perf(api): 透传 max_time_seconds 并为测试降低求解时间上限以加速后端 CI`
- 4 files changed, 30 insertions(+), 22 deletions(-)
- 改动文件：`apps/api/app/services/schedule_service.py`、`apps/api/app/scheduling/eligibility.py`、`apps/api/tests/test_schedule_pipeline.py`、`apps/api/tests/test_draft.py`
- 提交前完整跑通 CI 全部 6 项：前端 typecheck/test(15)/build + 后端 ruff format-check/ruff check/pytest(194) 全绿
- `.workbuddy/` 为未跟踪本地目录，未纳入提交
- 已 push 到 `origin/main`（`2cdaeda..a94314d`）

## 移除 Redis 并审计依赖（未 commit）

### 改动
- `apps/api/pyproject.toml`：删除 `redis>=5.2` 与 `python-dateutil>=2.9` 两个依赖
- `apps/api/uv.lock`：随 `uv sync` 更新
- `apps/api/app/core/config.py`：删除 `redis_url` 配置项
- `apps/api/app/core/ratelimit.py`：移除 Redis 分支，改为纯进程内存限速（登录失败计数，5 次/300s 锁；多 worker 下各进程独立计数）
- `README.md` / `docs/deployment.md`：架构图与组件表去掉 redis
- `uv sync --all-extras --dev` 已执行，环境卸载 redis、python-dateutil（`import redis` 现报 ModuleNotFoundError）

### 依赖审计结论
- 移除：`redis`（用户要求）、`python-dateutil`（全仓零使用，死依赖）
- 保留并核实在用：`cryptography`(AESGCM 字段加密)、`pypinyin`(拼音排序)、`openpyxl`(Excel 导入导出)、`python-multipart`(UploadFile 文件上传)、`ortools`(排班求解)、`apscheduler`(周期任务)、`pymupdf`(PDF 解析)
- 注意：`会议场地排班系统_完整实施方案.md` 仍描述旧 Celery+Redis 架构（从未实现，实际用 APScheduler），属历史设计文档，未改动

### 校验
- 后端 ruff format/check 全绿，pytest 194 passed（24.8s）
